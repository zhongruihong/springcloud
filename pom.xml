<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.rui</groupId>
	<artifactId>springcloud</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<properties>
		<project.build.resourceEncoding>uft-8</project.build.resourceEncoding>
		<jdk.version>1.8</jdk.version>
		<druid.version>1.1.10</druid.version>
		<spring-cloud.version>Greenwich.RELEASE</spring-cloud.version>
		<spring-boot.version>2.1.8.RELEASE</spring-boot.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<!-- cloud -->
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<!-- Greenwich only supports Spring Boot 2.1.x ,if useing 2.0.x,change to Finchley -->
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- boot --><!-- add cloud first，or boot can't dependent -->
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId><!-- so many dependencies！ -->
				<version>${spring-boot.version}</version>
				<scope>import</scope>
			</dependency>
			<!-- druid -->
			<dependency>
				<groupId>com.alibaba</groupId>
				<artifactId>druid</artifactId>
				<version>${druid.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<!-- for setting files under src/main/resources,such as application.yml for $ begin or end -->
	<build>
		<finalName>springcloud</finalName>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
			</resource>
		</resources>
		<!--jdk -->
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<configuration>
					<source>${jdk.version}</source>
					<target>${jdk.version}</target>
					<encode>${project.build.resourceEncoding}</encode>
					<delimiters>
						<delimit>$</delimit>
					</delimiters>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>${jdk.version}</source>
					<target>${jdk.version}</target>
					<encode>${project.build.resourceEncoding}</encode>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<modules>
		<module>common</module>
		<module>config-client-5001</module>
		<module>config-server-4001</module>
		<module>consul-customer-8601</module>
		<module>consul-producer-8501</module>
		<module>consul-producer-8502</module>
		<module>eureka-config-server</module>
		<module>eureka-consumer-8081</module>
		<module>eureka-consumer-feign-8082</module>
		<module>eureka-customer-hystrix-feign-8083</module>
		<module>eureka-gateway-8001</module>
		<module>eureka-hystrix_turbine-cluster-9091</module>
		<module>eureka-provider-1001</module>
		<module>eureka-provider-1002</module>
		<module>eureka-provider-1003</module>
		<module>eureka-provider-config</module>
		<module>eureka-provider-hystrix-1004</module>
		<module>eureka-provider-hystrix-1006</module>
		<module>eureka-provider-hystrix-feign-1005</module>
		<module>eureka-server-2001</module>
		<module>eureka-server-2002</module>
		<module>eureka-server-2003</module>
		<module>eureka-zuul-3001</module>
		<module>gateway-byJavaOrXml</module>
		<module>hystrix_dashboard-single-9090</module>
	</modules>
</project>